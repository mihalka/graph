name: Build and Publish Docker
on:
  push:
    branches:
      - master
jobs:
  job_1:
    runs-on: ubuntu-latest  
    steps:
      - id: login
        shell: bash
        run: echo ${{ secrets.DOCKER_PWD }} | docker login -u ${{ secrets.DOCKER_LOGIN }} --password-stdin
      - id: build
        shell: bash
        run: docker build -t b094d7b21ad5/graph .
      - id: push
        run: docker pull b094d7b21ad5/graph


          
  
   
   
        
        
      

